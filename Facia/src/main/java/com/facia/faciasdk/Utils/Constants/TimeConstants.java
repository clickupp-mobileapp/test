package com.facia.faciasdk.Utils.Constants;

import java.util.concurrent.TimeUnit;

public class TimeConstants {
    public static final int SYNCHRONIZED_CONSTANT = 250;
    public static final int RESULT_API_DELAY_ONCE = 4000;
    public static final int INIT_OVAL_UI_AT_START = 2000;
    public static final int UPLOADER_GONE_EXTRA_TIME = 2000;
    public static final int OVAL_ANIMATION_TIME = 400;
    public static final int ADDITIONAL_VERIFICATION_DELAY = 2500;
    public static final int REMOVE_RESULT_SCREEN = 3000;
    public static final int REMOVE_QL_INSTRUCTIONS_SCR = 3000;
    public static final int SHOW_CAPTURE_DOC_BTN_DELAY = 5000;
    public static final double QL_AND_DL_TIMEOUT = TimeUnit.MILLISECONDS.toMillis(15000);
    public static final double ADD_IN_QL_LIST_DELAY = TimeUnit.MILLISECONDS.toMillis(334);
    public static final double DL_BIG_OVAL_TIMEOUT = TimeUnit.MILLISECONDS.toMillis(7000);
    public static final double WAIT_FOR_BLINKING = TimeUnit.MILLISECONDS.toMillis(2000);
    public static final double HOLD_STEADY_IN_OVAL_DL = TimeUnit.MILLISECONDS.toMillis(1000);
    public static final double HOLD_STEADY_IN_OVAL_QL = TimeUnit.MILLISECONDS.toMillis(500);
    public static double BLINK_DETECTION_TIMER = TimeUnit.MILLISECONDS.toMillis(4000);
    public static double MIN_BLINK_DETECTION_TIMER = TimeUnit.MILLISECONDS.toMillis(1000);
    public static final double BLINK_TIMEOUT = TimeUnit.MILLISECONDS.toMillis(15000);
}
